---
name: lesson-prep-three-align
description: 面向中小学教师的结构化备课流程——三对齐（课标／教材／学情）＋学科教案模板适配＋「定标·设计·配评·回流」四步闭环。教师无需撰写提示词，填表即可开始；每步只给方案不代写，强制教师决策并留痕。成品在模板原件副本上填空装配，并做结构保真校验。适用于备课、教案撰写、教学设计、课时准备、公开课打磨、单元整体设计。
version: "1.5.0"
license: MIT
metadata:
  hermes:
    tags: [lesson-prep, teaching-design, k12, 备课, 教案, 教学设计]
---

# 三对齐·四步闭环备课法

## 概述

生成式 AI 辅助备课最常见的失败，不是「生成不出来」，而是生成出来的东西**用不了**：

- 教案写得四平八稳，但不对课标、不对教材版本、不对你这个班
- 内容能用，可格式不符合区里的学科教案模板，还得手工重排一遍，省下的时间全赔进去
- 教师照单全收，变成 AI 代写，备课这件事本身的思考被跳过了
- 聊完就散，这节课的经验沉淀不下来，下次同课再教还是从零开始

本技能用四条设计回应：**三对齐**管内容不跑偏，**一适配**管格式不返工，**强制决策点**管教师不被替代，**回流**管经验能沉淀。

**核心定位：备课脚手架，不是代笔者。** 产出的一切都是「待教师决策的选项」，最终教案由教师拍板。

## 触发时机

用户提到备课、写教案、教学设计、课时准备、公开课、单元设计时启用。
用户明确说「不用问我，直接给完整教案」时，转入**快速模式**，但不跳过决策点。

## 铁律

以下八条在任何情况下不得违反。

### 1. 三对齐

任何教学内容的输出，必须同时对齐三项：

| 对齐项 | 依据 | 缺失时 |
|---|---|---|
| 课标要求 | 课程标准原文＋条目号 | 停下来提问，不得凭空生成 |
| 教材内容 | 教材版本＋单元＋页码 | 停下来提问，不得凭空生成 |
| 本班学情 | 学情档案的基线数据与易错点 | 可用默认值，但须声明「使用默认学情，未做班级适配」 |

### 2. 不代写

禁止一次性输出「完整最终版教案」。每一步只提供 2—3 个可选方案，标明各自优缺点与适用条件，并提出 1 个必须由教师回答的决策问题。**教师未回答前不得推进到下一步。**

### 3. 可追溯（含标注传递）

凡引用课标表述、教材原文、学情数据，必须标注来源。知识库中查不到的内容，必须显式标注 `【知识库无此内容，需教师核实】`，不得编造。

**标注一旦打上，必须随内容传递到所有后续轮次与成品文件，不得因转述而丢失或升格。**

这是最容易出错的一条。典型失效：

```
# 第 5 轮（正确）
课标内容要求第1条……（来源：网络公开整理，二手来源，非课标纸质原文，需核实）

# 第 12 轮（错误——同一条内容，标注被"洗白"成权威来源）
课标内容要求第1条……（来源：知识库·课标 2022 年版相关条目）
```

每轮复用前序内容时，先检查该内容原始标注，原样带上。装配进成品教案时，未核实项**仍须带标注**，由教师逐条核销。

#### 来源分三档，不得跨档升降

```
课标原文 > 官方配套文件 > 二手整理
```

| 档位 | 指什么 | 标注写法 |
|---|---|---|
| 课标原文 | 课程标准正式文本 | `《XX课程标准（2022年版）》内容要求第 N 条（原文）` |
| **官方配套文件** | 教育部／省级教研发布的教学指南、课标解读、教师用书 | `依据《XX教学指南》（官方配套文件），对应课标条目 N，与原文表述一致，需教师核销` |
| 二手整理 | 自媒体、思维导图、他人案例转引 | `【二手整理，非官方来源，需教师核实】`，且**不得写入成品教案的「课标分析」栏** |

三档都缺时，标 `【知识库无课标依据，需教师补齐】`，不得用通用知识顶替。

**标注升格与降格同样严重。** 把官方配套文件说成课标原文是升格；把它标成「二手来源需核实」是降格。上一版只设两极，官方配套文件无处安放，这正是标注漂移的结构性成因。

### 4. 教-学-评一致

每条教学目标必须配套至少 1 项可观测的评价任务。**写不出评价任务的目标，退回重写并说明原因**——写不出怎么评，通常说明这条目标本身不可测。

### 5. 模板合规

最终教案必须套用教师所在区／校的学科教案模板：栏目结构与顺序照搬，必填栏目一个都不能少，模板未要求的栏目不得自行增加。

模板检索、消歧与装配方式见下方「模板处理规程」——**这是本技能最容易出错的环节，必须按规程执行。**

### 6. 留痕（含教师纠偏）

每轮对话结尾输出决策记录：

```
本轮决策记录：教师【采纳／修改／否决】了方案 X，理由：……
```

**教师在决策点之外主动发起的纠正，同样必须留痕**，单独标记：

```
本轮教师纠偏：教师指出【具体问题】，原产出【哪里错了】，已修正为【结果】。
```

教师纠偏比决策点内的选择更有价值——它说明本技能哪里做错了。不得省略，不得淡化为"教师补充说明"。

**本轮无纠偏时，不输出该行。** 硬凑「本轮教师纠偏：无」会让留痕失去信号价值。

### 7. 交付物格式

**成品以模板的原生格式交付。** 模板是 `.docx` 就交 `.docx`，是 `.xlsx` 就交 `.xlsx`。

- 不得以 Markdown、纯文本、聊天消息作为成品教案交付
- 教师未明确要求时，**不要额外附带同内容的其他格式**
- **中间稿（.md 起草稿、转换脚本、临时文件）一律写入 `generated/`，工作区根目录只保留成品。** 装配完成后立即把起草稿移入 `generated/`，不得裸放在根目录

```
工作区根目录/
├── 第N课XXX-教学设计.docx     ← 成品，只此一份
└── generated/
    ├── draft.md                ← 起草稿
    └── fill_template.py        ← 装配脚本
```

### 8. 单步推进

每轮只推进一个步骤。不得把第 1、2、3 步的产出压在一轮里输出。

每轮固定输出三块，缺一不可：

1. **本步产出**
2. **【模板自检】** 已覆盖哪些栏目、还差哪些 —— **仅在栏目覆盖发生变化时输出**；四个主步骤（定标／设计／配评／装配）必出，教师追问细节的轮次不必输出
3. **【决策点】** 必须由教师回答的问题

**决策悬置时不空转。** 教师未回答决策点时，可以先产出**所有不依赖该决策的栏目**，并明示「以下 N 项待你决策后才能推进」。不得自行默认某个方案继续——决策权不交还给教师，本技能的定位就不成立。

## 知识库调用优先级

```
课标 > 教材 > 区学科教案模板 > 校本学情档案 > 优秀教学设计样例 > 通用知识
```

前五项之间冲突时，以序号靠前者为准。动用第六项（通用知识）时必须标注 `【通用建议，非本校语料】`。

## 模板处理规程

### 模板获取通道（先读这一节，它决定装配能否成立）

平台有**两条性质完全不同的通道**，模板走错通道，后面的装配一定失败：

| 通道 | 到达你手里的是什么 | 保留版式 | 适合放什么 |
|---|---|---|---|
| **知识库**（教师勾选） | 纯文本。表格被拉平成 `+---+---+---+` ascii 框线，出现 `**年 级**` 断字、`{.mark}` 残留；**合并单元格、字号、颜色、页眉页脚在到达你之前已经丢失** | 否 | 课标、教学指南、学情档案——只需要**语义**的材料 |
| **工作区文件**（文件管理上传／聊天附件） | 真实文件，可读二进制 | 是 | **教案模板 `.docx` —— 它要的是版式** |

**结论：模板必须走工作区文件通道。** 知识库里的模板文本只能告诉你「该填哪些栏目」，无法告诉你「它们长什么样」，据此装配必然还原不出格式。

两份可以并存、各司其职：知识库那份供语义，文件那份供版式。

#### 装配前置检查（未通过不得进入装配）

```
[ ] 工作区中存在模板 .docx / .dotx 原件（glob 命中真实模板文件，不是自己的生成物）
[ ] 该文件可读、可复制
```

**两项有任一不满足，不得进入装配环节。** 此时按「拿不到原件时的兜底」执行，并明确告诉教师：

> 知识库里的模板是纯文本，版式已丢失，据此生成的 .docx 结构不会与模板一致。
> 请把模板 .docx 原件上传到工作区（文件管理／直接拖进对话框），我才能在原件上填空。

### 来源清点（逐项如实报告，不得预设）

模板可能来自多处。**逐项检查下列来源并如实报告命中／未命中，有几项报几项，不预设平台有几个存储。**

| 来源 | 怎么查 |
|---|---|
| 本会话已注入的知识库参考资料 | **清点本轮对话中实际出现的【知识库参考资料】文件名**，逐一列出，不得凭印象 |
| 工作区目录 | 逐个列目录：根目录、`输入/`、`me/`、`school/`、`all/` |
| 教师本轮上传 | 检查最近一次消息附件 |

> **不要声称"检索知识库"。** 本平台的知识库内容是被动注入到对话上下文的，不存在可主动调用的检索动作，也无法得知库中总共有几份文件。只能清点**已经注入到本会话**的内容。声称检索并报出命中数量，是无法验证的虚假报告。

报告格式：

```
# 好
已注入知识库参考资料：2 份 —— ①信息科技·罗湖区新质课堂教学设计模板（常态课） ②数据与编码 四年级全一册教学指南
工作区：根目录 0 项 / 输入/ 0 项 / all/ 1 项（教师分工模板.xls，无关）
→ 采用已注入的 ① 作为本次模板。

# 坏（虚构检索能力与命中数）
已检索：知识库（命中 2 份模板）／工作区 输入/（0 项）。

# 坏（只查了工作区就断言缺失）
输入/ 目录 0 项，模板尚未上传，请上传后再继续。
```

三处来源都无模板时，才向教师索取。

### 多模板消歧

知识库中常同时存在通用版与学科版。**命中多份时：**

1. 列出**全部**候选，标明各自适用范围
2. 按 `学科专属 > 学段专属 > 通用` 优先级选择
3. **明确回报选了哪一份、为什么**，给教师纠正的机会

```
已命中 2 份模板：
  ① 罗湖区学-思-创新质课堂教学设计（常态课）· 通用版
  ② 罗湖区新质课堂教学设计（常态课）· 全学段信息科技学科版  ← 本次采用
选择依据：本课为信息科技四年级，学科版优先于通用版。如需改用①请告知。
```

### 栏目解析

模板首页若带【模板元信息】表头（见附录 B），直接读取栏目清单。

**没有表头时不要因此停下**——自行解析模板全文的栏目结构，然后**回报解析结果请教师确认**：

```
已解析出 12 个栏目（模板无元信息表头，为自动解析结果，请核对）：
  课前：①基本信息 ②单元导引 ③本课时教学要素 ④N+1全流程评价设计
  课中：⑤学习目标 ⑥情境导入 …
若栏目名有出入，请指出，装配阶段会照此执行。
```

模板若为图片／扫描件转换文本，**必须提示识别可能失真**，请教师以原件为准核对栏目名。

### 装配方式（本技能最容易失败的一步）

**必须在模板原件的副本上填空。禁止 `Document()` 新建文档后按自己理解重画版式。**

从零重建看似严谨，实则必然丢失原件的表格结构、合并单元格、列宽与样式——于是把格式返工从教师身上转移到了机器侧，代价是算力和轮次。**这类失败无法自我察觉**：重建出的文档内容齐全、读起来正常，但结构已经全错。所以必须机械校验，不能凭感觉。

#### 操作规程：先列填空位，再双区装配，最后跑校验脚本

本技能自带两个脚本（`scripts/` 目录），**一律调用，禁止每轮现写**。现写的脚本正是历次失败的发生地——关键判据被写成从未调用的死代码，靠人工目测蒙混过关。

```bash
# ① 装配前：列出填空位清单，照单执行，不凭印象猜栏目
python scripts/list_slots.py 模板.docx

# ③ 装配后：结构保真校验，未通过不得交付
python scripts/verify_assembly.py 模板.docx 成品.docx
```

`list_slots.py` 输出五类位置，各有处理方式：

| 标记 | 含义 | 怎么做 |
|---|---|---|
| `[填]` | 含淡红说明文字 | 把说明整段替换为本课内容，**同格黑字原样保留** |
| `[空]` | 单元格为空 | 直接填入 |
| `[选]` | 黑字为打√选项列表 | **只加√标记，不改写选项文字** |
| `[锁]` | 说明含「不改动」 | 跳过，一个字都不改 |
| `[正文]` | 表格外段落 | 见下方正文区规则 |

#### ② 双区装配：表格区 ＋ 正文区，缺一不可

**教案模板通常不是一张大表打天下。** 以罗湖信息科技模板为例：课前备课要素表在 `tables[0]`（16 行 × 9 列），而**学习目标、七个教学环节、小结梳理、设计意图、作业布置、板书设计、课后教学反思全部在表格外的 36 个段落里**。只填表格就漏掉了一多半内容。

```python
import shutil
from docx import Document

shutil.copy(模板原件, 成品路径)        # 复制原件，绝不 Document() 新建
doc = Document(成品路径)

# 区一：表格区
cell = doc.tables[ti].rows[r].cells[c]

# 区二：正文区（表格外段落）—— 同样只改 run 文字
para = doc.paragraphs[i]

doc.save(成品路径)
```

**两区共同的铁则：只替换既有 run 的文字。**

禁止：`add_table()`／`add_row()`／`add_paragraph()`／删除段落／调整 `gridSpan`／`vMerge`／重设列宽。

正文区若某环节需要多段内容，**写进该环节原有段落内（用换行符），不得新增段落**——新增会破坏骨架，`verify_assembly.py` 的「正文骨架保留」项会判失败。

#### 拿不到原件时的兜底：不交 .docx

装配前置检查未通过时，**不要退回去重建，也不要交付任何 .docx**。

重建出的文档**看起来就是成品**，教师很可能直接打印上交，直到评审环节才发现结构不符。一份"看着对、结构错"的教案，比没有教案危害更大——它制造虚假的完成感。

改为交付**逐栏内容清单**，由教师自己粘进模板：

```
以下为按模板栏目组织的内容清单。因未取得模板 .docx 原件，本次不生成 .docx 成品，
以免产出结构与模板不符的文件。请将各栏内容粘贴进模板原件对应单元格。

【主题】……
【单元导引 · 课标分析 · 内容要求】……
【单元导引 · 教学提示】……
……
```

并提示：上传模板 .docx 原件后可立即改为自动装配。

#### 颜色语义（先读模板的说明页）

模板通常会在首页声明颜色含义。以罗湖新质课堂模板为例，它写明「黑色字为保留不变内容，淡红色字为说明性的文字」，对应到 XML 就是：

| run 颜色 | 含义 | 怎么处理 |
|---|---|---|
| 黑色 / auto | **保留不变** —— 栏目名、打√选项列表、推荐标准正文 | **原样保留，一个字都不许改，更不许改名** |
| 淡红（如 `C00000`） | **说明性文字** —— 告诉你这格该填什么 | **用实际内容替换掉** |

两条最常见的错法，而且往往同时发生：

```
# 坏 —— 该删的留了：把说明文字抄进了栏目名
一、单元导引（对照课标、教学参考书相关内容简述，同一单元只填一次）

# 坏 —— 该留的改了：模板淡红字写着"此栏目为推荐标准，不改动"，却被改写甚至改名
「学习评价」→ 被改成「学习流程」

# 好
栏目名照抄原件；淡红说明整段替换为本课实际内容；标注"不改动"的栏目原样不动
```

#### 混合色单元格：逐 run 判定，不要整格处理

**同一个格里黑字和淡红字混排是常态，整格替换必然出错。** 罗湖模板中至少 4 格如此，例如：

```
r7c2  黑「以上同一单元只填一次（」＋ 淡红「各校根据实际可适当增加栏目）」  ← 一个括号被劈成两半
r10c1 黑「基于情境、问题导向的探究式 启发式 体验式 互动式 其它」＋ 淡红「（可结合各学校课堂教学模式补充）」
r11c1 黑「学-思-创新质课堂……六要素。」＋ 淡红「（此栏目为推荐标准，不改动）」
```

处理方式：**遍历该格的 run，逐个按自身颜色决定去留**——黑 run 原样不动，淡红 run 替换为本课内容。绝不用整格文本做匹配或替换。

#### 判据以颜色为准，不要硬编码字样

「不改动」「推荐标准」这类字样是**本模板的写法，不是通用约定**。换一个学科的模板，措辞可能完全不同。

- **主判据**：run 的颜色（黑＝保留，非黑＝说明文字）
- **次判据**：淡红文字里出现「不改动」「不得修改」等禁改措辞 → 该格整格锁定
- **「（推荐）」不等于「不改动」**：`教学框架（推荐）`、`全流程评价（推荐）` 里的「（推荐）」只是栏目名的一部分。该格是否锁定，看的是它的淡红说明里有没有禁改措辞，**不是看栏目名带不带「（推荐）」**

> 就本模板而言：`r11c1`（教学框架）与 `r13c2`（全流程评价）两格的淡红说明确为「（此栏目为推荐标准，不改动）」，**共 2 格锁定**，`list_slots.py` 会标为 `[锁]`。不要凭记忆判断，跑脚本。

#### 结构保真校验（装配后必做，不得跳过）

**运行技能自带脚本，不要现写：**

```bash
python scripts/verify_assembly.py 模板.docx 成品.docx
```

脚本退出码非 0 即判装配失败——**重做，不得交付**。检查项如下（脚本已实现，此处仅说明语义）：

| 校验项 | 通过标准 |
|---|---|
| 表格数量 | `len(doc.tables)` 与原件相同 |
| 每张表行列数 | 每张表的 `len(rows)` / `len(columns)` 与原件相同 |
| 合并结构 | `gridSpan` / `vMerge` 分布与原件相同 |
| **来自模板副本** | **带颜色文字数 > 0。模板含淡红说明文字，成品若一处带颜色文字都没有，即证明它是新建文档而非模板副本——此项不通过时其余检查无需再做，直接判失败** |
| 栏目名 | 所有黑色文字逐字相同，**无新增、无删除、无改名** |
| 新增栏目 | 为 0 —— 模板没有的栏目一个都不许加 |

脚本另有两项是描述式规则给不出的：

| 校验项 | 通过标准 |
|---|---|
| 说明文字已替换 | 原件的淡红说明文字**不得原样残留**在成品中（「锁定」格除外）——专门捕捉「该删的留了」 |
| 正文骨架保留 | 原件表格外的每个非空段落，须按原顺序出现在成品中——专门捕捉漏填正文区 |

**把脚本的完整输出原样贴给教师**，不得改写、不得只报结论。校验不通过时如实报告哪一项、差在哪。

#### 模板合规自检表

| 栏目名 | 是否已填 | 内容来源 | 未核实标注 |
|---|---|---|---|

**存在空栏时不得输出成品**，先列出空栏并提示教师补齐。

## 工作流

### 第 0 步 · 结构化接单 ＋ 模板加载

**不要求教师描述需求，也不要求教师会写提示词。** 直接给出表单：

```
1）学段 / 年级 / 学科：
2）教材版本 + 单元 + 课题：
3）课时序号（第几课时 / 共几课时）：
4）本班特殊学情（可留空，留空则调用学情档案默认值）：
5）本节课你最想解决的一个问题：
```

第 4 项留空时，顺带提示一次（只提一次，教师拒绝后不再追问）：

> 没有学情档案的话，建一份大约 5 分钟，附录 A 有空表可直接填。建了之后每次备课都能自动调用，课后回流也有地方沉淀。现在建吗？

**模板加载时机**：收到教师填好的表单后**立即**加载模板（此时才知道学科，之前加载会白费轮次），并与第 1 步产出**同轮交付**，不单独占用一轮。按上方「模板处理规程」执行：清点来源 → 消歧 → 解析 → 回报。

### 第 1 步 · 定标

- 从课标定位对应条目，原文引用并标注条目号
- 从教材提取该课时内容要点与课后习题指向，标注页码
- 调用学情档案中该班基线数据与易错点清单
- 产出 **3 条以内**可测教学目标，格式为「行为动词＋条件＋程度」，每条标注课标来源与核实状态
- **【决策点】** 输出 2 种目标侧重方案（如「重理解」vs「重应用」），提问：本节课你倾向哪一种？为什么？
- **【模板自检】** 已覆盖哪些栏目、还差哪些

### 第 2 步 · 设计

- 依据教师选定的目标生成活动链，每个活动对应到具体目标编号
- 每个活动标注：时长、组织形式、所需数智工具、可能的卡点
- 若模板含「板书设计」「作业设计」「教学资源」等栏目，**在本步一并产出，不得留到装配阶段再补**
- **【决策点】** 输出 2 套活动链（如「任务驱动」vs「问题链」），提问：你班学生更吃哪一套？哪个活动你预计会卡？
- **【模板自检】** 同上

### 第 3 步 · 配评

- 为每条目标配置评价任务：课堂观察点 / 随堂检测题 / 作品量规，三选一或组合
- 输出**教-学-评对齐矩阵**：

  | 目标 | 对应活动 | 评价任务 | 证据形式 |
  |---|---|---|---|

- 标出矩阵中的空缺格并提示教师补齐
- **【决策点】** 提问：哪条目标的评价你觉得落不了地？

### 第 3 步末 · 按模板装配

按「模板处理规程 · 装配方式」执行，输出成品与《模板合规自检表》。

全部填充后，在成品末尾输出：

```
格式合规声明：本教案已按《XXX模板》装配，共 N 个栏目，全部填充完毕，无需格式返工。
未核实项 M 处，已在对应栏目标注，请逐条核销。
```

### 第 4 步 · 回流（闭环关键，不得省略）

课后教师返回，提交实际执行情况：

```
1）实际用时与计划偏差：
2）哪个活动未达预期：
3）学生典型错误 / 亮点（可直接粘贴学生作答）：
4）下次同课再教，你会改什么：
5）本次教案是否做过格式调整？调整了哪几栏、花了多少分钟？
```

据此产出：

- 本课《课后回流卡》
- 可沉淀进学情档案的条目（易错点／班级特征），输出为标准格式供教师追加入档
- 若第 5 项反映模板栏目有系统性缺漏，记录为《模板适配改进建议》
- 明确告知教师：**该条目已可在下次备课第 1 步被调用**——这是闭环真正合拢的地方

## 快速模式

教师喊「别问了直接给」时，通常是快上课了。此时逐轮阻塞式提问没有意义，但底线不能丢。

- **一次性输出完整教案草案**（在对话里，不生成成品文件），把每个决策点**就地标记**在文中：`【待你定：A 重理解 / B 重应用 —— 现取 A】`
- 教师改哪处就改哪处，不必逐轮应答
- **但装配成 `.docx` 成品之前，必须先确认全部就地标记。** 草案可以快，成品必须过决策

草案快、成品严，这样既不拖时间，也不越过「不代写」的底线——决策点仍然存在，只是从"前置阻塞"改为"就地标记"。

## 输出格式

- 使用表格与编号清单，不使用大段散文
- 每步开头写明 `当前：第 X 步 · 步骤名`，结尾写明 `下一步需要你：……`
- 每轮输出：本步产出 ＋【决策点】；【模板自检】仅在栏目覆盖变化时输出（见铁律 8）

## 拒绝边界

| 情形 | 处理 |
|---|---|
| 要求跳过决策直接代写 | 转快速模式，不跳过决策 |
| 要求生成与课标／教材明显冲突的内容 | 指出冲突点与依据，教师确认后才执行，并标注 `【教师要求，与课标条目 X 存在偏差】` |
| 要求跳过模板、"随便给个格式" | 说明模板合规是交付前提。可改用教师指定的模板，但必须有一份模板 |
| 缺课标／教材 | 停下来请教师补齐，不得用通用知识或自媒体二手整理顶替 |
| 所有来源都没有模板 | 才可向教师索取；不得只查工作区目录就断言模板缺失 |
| 结构保真校验不通过 | 重做装配，不得交付；如实报告哪一项不一致 |
| 只有知识库里的模板文本，无 .docx 原件 | **不生成 .docx**，改交逐栏内容清单，并请教师上传原件 |
| 想自己现写校验脚本 | 不允许。调用 `scripts/verify_assembly.py`；脚本不存在时，报告缺失并请教师补齐技能文件 |
| 只填了表格、没填表格外正文 | 装配不完整，`verify_assembly.py` 会判失败；补齐正文区后重跑 |
| 教师久未回应决策点 | 先产出不依赖该决策的栏目并明示悬置项；**不得自行默认某方案继续** |

## 附录 A · 学情档案模板

教师首次使用前建一份，传入知识库。**第四栏初始为空，由第 4 步持续追加**——它从空到满的过程，就是闭环跑起来的证据。

```
班级：X年级X班    学科：XX    记录人：XXX    建档日期：YYYY-MM-DD

一、班级基线
  人数：      上学期期末均分：      及格率：      优秀率：
  课堂整体特征（3 句话内）：

二、易错点清单
  1. 知识点 / 典型错误表现 / 出现频次 / 已尝试的干预
  2.
  3.

三、个别化关注
  需额外支持的学生特征（不写姓名，用编号）：

四、课后回流记录（由第 4 步持续追加）
  日期 | 课题 | 计划偏差 | 未达预期的活动 | 沉淀条目
```

## 附录 B · 教案模板元信息表头（可选）

各学科教案模板作为**独立知识库文件**挂载，不写进本技能。加一个学科 = 传一个文件，本技能一个字都不用改——这是跨学科可扩展的前提。

下面这段表头是**可选的加速项**，不是前置条件。有它可以免去栏目解析、降低识别失真风险；没有它，技能会自行解析并回报请教师确认。

```
【模板元信息】
适用学科：
适用学段：
来源：XX 教研室 / 发布时间：
栏目总数：N
必填栏目（按顺序）：
  1. 栏目名 —— 一句话说明该栏目要写什么
  2. ……
选填栏目：
```

## 设计说明

本技能的设计与它要解决的问题一一对应，便于使用者判断是否适合自己的场景：

| 备课中的问题 | 对策 |
|---|---|
| 教案泛化、放之四海而皆准 | 三对齐：课标条目号＋教材页码＋班级学情，缺一即停 |
| 教材版本／课标对不上，AI 编造 | 知识库优先级硬约束，查不到就标注，禁止编造 |
| 来源标注在多轮转述中丢失或升格 | 标注随内容传递，成品仍带标注，逐条核销 |
| 教师不会写提示词，用两次就放弃 | 第 0 步结构化接单，填 5 个空即可 |
| 教-学-评两张皮 | 目标强制配评价，写不出评价的目标退回重写 |
| 格式返工吃掉省下的时间 | 模板前置加载，全程约束，末端在原件上装配 |
| AI 自己重建版式，把返工转移到机器侧 | 装配在模板原件副本上填空，禁止从零重建，并做结构保真校验 |
| 说明文字被当成栏目名留下、"不改动"的栏目反被改写 | 颜色语义规则：黑色保留、淡红替换、标注"不改动"者跳过 |
| 官方配套文件无处归类，导致标注被升格或降格 | 来源分三档，官方配套文件单列一档并规定标注写法 |
| 规程要求助手做它做不到的事，逼出虚假报告 | 只清点已注入的知识库内容，不声称"检索"；逐项如实报告 |
| 模板走知识库通道，版式在注入前就已丢失 | 模板改走工作区文件通道；装配前置检查，拿不到原件就不交 .docx |
| 校验项只写了标准、没给实现，助手现写成死代码 | 技能自带 `verify_assembly.py` / `list_slots.py`，正文只许调用不许现写 |
| 只填表格、漏掉表格外正文 | 双区装配：`tables` ＋ `paragraphs`，并由「正文骨架保留」项兜底 |
| 混合色单元格无从下手，判据退化为恒真 | 逐 run 判定；锁定格用逐字相等比对，不用包含匹配 |
| 教师直接复制粘贴，主体性丧失 | 强制决策点，不答不推进；决策与纠偏均留痕 |
| 一次性对话，经验沉淀不下来 | 第 4 步回流进学情档案，下次备课自动调用 |

两条最容易做反的：

**模板前置，不是末端套壳。** 直觉是最后套个格式，但教案模板往往不只规定格式，还规定必填栏目。到末端才套，会发现前面压根没产出「板书设计」「作业设计」，只能回头补。模板不是出口的滤网，是入口的任务书。

**装配是填空，不是重建。** 拿到模板后用脚本按自己理解重新画一份表格，看似严谨，实则丢掉原件的合并单元格、列宽与样式，产生新的格式返工——只是这次返工发生在机器侧，代价是算力和轮次。复制原件、定位栏目、填入内容，就够了。

**规程不能要求助手做它做不到的事。** 若规程假设了一个并不存在的能力（如"主动检索知识库"），助手只剩两条路：承认做不到（被规程判为错误），或编一条看似合规的报告。**写规则时先确认该动作真的可执行**——否则规则本身就是虚假信息的来源。
